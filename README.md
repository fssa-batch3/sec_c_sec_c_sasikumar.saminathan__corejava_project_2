# sec_c_sec_c_sasikumar.saminathan__corejava_project_2

```sql
CREATE TABLE products (
    product_id INT NOT NULL AUTO_INCREMENT,
    product_name VARCHAR(100) NOT NULL UNIQUE,
    product_price VARCHAR(100) NOT NULL,
    product_ram INT NOT NULL,
    product_storage INT NOT NULL,
    product_highlights VARCHAR(400) NOT NULL,
    product_quantity INT NOT NULL,
    product_brand VARCHAR(100) NOT NULL,
    PRIMARY KEY (product_id)
); 
css
Copy code

You can copy and paste this Markdown code into your document to render the SQL `CREATE TABLE` statement in a well-formatted code block. Just make sure to surround the code block with three backticks (```) before and after the code.




